name: feature_spotlight
description: A simple, declarative Flutter package for creating guided tours and feature showcases.
version: 1.0.0
homepage: https://github.com/febin52/feature_spotlight


environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
